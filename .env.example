# Contact form (Resend). Copy to .env.local and fill in.
# If RESEND_API_KEY is unset, the contact form degrades gracefully (no crash).
RESEND_API_KEY=
CONTACT_TO_EMAIL=ping@sherylabs.tech
CONTACT_FROM_EMAIL=onboarding@resend.dev
